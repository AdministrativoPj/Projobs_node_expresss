<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>ProJobs</title>

  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;500;700&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" />
  <link rel="stylesheet" href="/css/index.css">
  <link rel="stylesheet" href="/css/mensajes.css">
</head>
<body>

  <!-- NAVBAR -->
  <header class="navbar">
    <div class="logo">
      <img src="/img/logotipo.png" alt="Logo" />
      <span>ProJobs</span>
    </div>

    <i class="fas fa-bars menu-toggle" onclick="toggleMenu()"></i>

    <nav class="nav-links" id="navLinks">
      <a href="#quienes">¿Quiénes somos?</a>
      <a href="#servicios">Servicios</a>
      <a href="#contacto">Contactanos</a>
      <a class="btn-auth" href="/login">Iniciar sesión</a>
      <a class="btn-auth" href="/register">Registrarse</a>
    </nav>
  </header>

  <!-- CONTENIDO PRINCIPAL -->
  <main>
    <section id="hero" class="hero-section">
      <h1>Encuentra el talento ideal<br /><span>para tu próximo proyecto</span></h1>
      <p>Conectamos profesionales con oportunidades reales</p>
    </section>

    <section id="quienes">
      <div class="container">
        <h2>¿Quiénes somos?</h2>
        <p>Somos una plataforma que une trabajadores independientes con clientes que necesitan servicios de calidad.</p>
      </div>
    </section>

    <section id="servicios">
      <div class="container">
        <h2>Servicios</h2>
        <ul>
          <li>Diseño gráfico</li>
          <li>Desarrollo web</li>
          <li>Servicios técnicos</li>
          <li>Y mucho más...</li>
        </ul>
      </div>
    </section>

    <section id="contacto">
      <div class="container">
        <h2>Contactanos</h2>

        <% if (mensaje) { %>
          <p style="color: green;"><%= mensaje %></p>
        <% } %>
        <% if (error) { %>
          <p style="color: red;"><%= error %></p>
        <% } %>

        <p>Déjanos tu mensaje y nos pondremos en contacto contigo.</p>
        <form method="POST" action="/contact">
          <input type="text" name="nombre" placeholder="Tu nombre" required />
          <input type="email" name="correo" placeholder="Correo electrónico" required />
          <textarea name="mensaje" placeholder="Tu mensaje" rows="5" required></textarea>
          <button type="submit">Enviar</button>
        </form>
      </div>
    </section>
  </main>

  <footer>
    <div class="container">
      <p>&copy; <%= new Date().getFullYear() %> ProJobs. Todos los derechos reservados.</p>
    </div>
  </footer>

  <script>
    function toggleMenu() {
      const nav = document.getElementById('navLinks');
      nav.classList.toggle('active');
    }
  </script>
</body>
</html>
